// Global type definitions
export type { Theme } from './theme'

// Re-export commonly used types from lib
export type {
  Database,
  Tables,
  UserPreferences,
  FormSection,
  DailyEntry
} from '@/lib/types'
